# Use postgres/example user/password credentials
version: '3.1'

services:

  db:
    image: logdb
    restart: always
    environment:
      POSTGRES_PASSWORD: passwordlogdb

  adminer:
    image: loggerdb
    restart: always
    ports:
      - 3000:3000